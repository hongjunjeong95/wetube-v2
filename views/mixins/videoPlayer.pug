mixin videoPlayer(video={})
  .videopPlayer
    video(src=`/${video.videoUrl}`)
    .videoPlyaer__controls
      .videoPlayer__column
        span#jsVolumeBtn
          i.fas.fa-volume-up
      .videoPlayer__column
        span#jsPlayBtn
          i.fas.fa-play
      .videoPlayer__column
        span#jsFullScreen
          i.fas.fa-expand